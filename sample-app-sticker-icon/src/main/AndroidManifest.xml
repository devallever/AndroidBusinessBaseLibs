<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT"/>
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>

    <!-- Required to maintain app compatibility. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BakerIcon"
        tools:targetApi="31">
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BakerIcon.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.alsg.bakericon.ui.PreviewActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.alsg.bakericon.base.AppFragmentActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.alsg.bakericon.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.alsg.bakericon.ui.BillingActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.alsg.bakericon.SlideMainActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>