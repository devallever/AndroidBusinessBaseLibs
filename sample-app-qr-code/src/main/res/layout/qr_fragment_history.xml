<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context="com.allever.app.qr.code.scaner.app.allever.android.ai.qr.scanner.ui.HistoryFragment">

    <app.android.base.lib.tab.TabLayout
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/qr_history_tab_background" />

    <FrameLayout
        android:id="@+id/adContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tabs"
        android:background="@drawable/qr_ad_container_bg"
        android:padding="0.5dp"
        android:visibility="gone"/>

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/adContainer"
        android:layout_above="@+id/rl_bottom_toolbar"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />


    <!--    <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="8dp"
            android:clickable="false"
            android:visibility="gone"
            android:clipToPadding="false"
            android:scrollbarStyle="outsideOverlay"
            android:scrollbars="vertical" />-->

    <RelativeLayout
        android:id="@+id/rl_bottom_toolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:background="@color/qr_history_bottom_background">

        <CheckBox
            android:id="@+id/cb_select_all"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginStart="@dimen/qr_history_list_margin"
            android:layout_marginLeft="@dimen/qr_history_list_margin"
            android:button="@drawable/qr_checkbox_style"
            android:paddingStart="10dp"
            android:paddingLeft="10dp"
            android:paddingEnd="20dp"
            android:paddingRight="20dp" />

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="48dp"
            android:layout_height="match_parent"
            android:layout_toStartOf="@+id/iv_delete"
            android:layout_toLeftOf="@+id/iv_delete"
            android:scaleType="centerInside"
            android:src="@drawable/qr_icon_back" />

        <ImageView
            android:id="@+id/iv_delete"
            android:layout_width="48dp"
            android:layout_height="match_parent"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_marginEnd="@dimen/qr_history_list_margin"
            android:layout_marginRight="@dimen/qr_history_list_margin"
            android:scaleType="centerInside"
            android:src="@drawable/qr_icon_del" />

    </RelativeLayout>

</RelativeLayout>
