<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/includeTopBar"
        layout="@layout/hdc_include_top_bar" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/searchInputContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@+id/includeTopBar">

        <View
            android:id="@+id/etContainer"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/hdc_shape_gray_frame_r45"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/ivSearch"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginHorizontal="16dp"
            android:src="@drawable/hdc_ic_search"
            app:layout_constraintBottom_toBottomOf="@+id/etContainer"
            app:layout_constraintStart_toStartOf="@+id/etContainer"
            app:layout_constraintTop_toTopOf="@id/etContainer" />

        <EditText
            android:id="@+id/etTableCode"
            android:inputType="number"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@null"
            android:gravity="center_vertical"
            android:hint="@string/hdc_input_table_num"
            android:maxLines="1"
            android:paddingHorizontal="16dp"
            android:singleLine="true"
            android:text=""
            android:textColor="@color/color_282929"
            android:textSize="18sp"
            app:layout_constraintBottom_toBottomOf="@+id/etContainer"
            app:layout_constraintEnd_toEndOf="@+id/etContainer"
            app:layout_constraintStart_toEndOf="@+id/ivSearch"
            app:layout_constraintTop_toTopOf="@+id/etContainer" />


    </androidx.constraintlayout.widget.ConstraintLayout>

    <View
        android:id="@+id/line"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@drawable/hdc_shadow_line"
        app:layout_constraintTop_toBottomOf="@+id/searchInputContainer" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvOrder"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/line"
        tools:itemCount="2"
        tools:listitem="@layout/hdc_item_order_manage" />

</androidx.constraintlayout.widget.ConstraintLayout>