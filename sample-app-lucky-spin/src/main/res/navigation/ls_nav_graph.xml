<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/nav_wheel">

    <fragment
        android:id="@+id/nav_wheel"
        android:name="app.allever.android.lucky.choice.spin.fragment.WheelFragment"
        android:label="Wheel"
        tools:layout="@layout/ls_fragment_wheel" />

    <fragment
        android:id="@+id/nav_finger"
        android:name="app.allever.android.lucky.choice.spin.fragment.FingerFragment"
        android:label="Finger"
        tools:layout="@layout/ls_fragment_finger" />

    <fragment
        android:id="@+id/nav_number"
        android:name="app.allever.android.lucky.choice.spin.fragment.NumberFragment"
        android:label="Number"
        tools:layout="@layout/ls_fragment_number" />

    <fragment
        android:id="@+id/nav_password"
        android:name="app.allever.android.lucky.choice.spin.fragment.PasswordFragment"
        android:label="Password"
        tools:layout="@layout/ls_fragment_password" />

</navigation>